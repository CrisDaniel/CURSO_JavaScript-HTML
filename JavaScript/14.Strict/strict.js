"use strict"    //Este argumetno entre comillas indica que sera necesario declara las variabler(let, var, const) para que funcione el codigo.
                //Es una manera estricta de programar sin olvidar las buenas practicas de declarar variables.
                //Intente borrar la variable (let) y al ejecutar le saldra error.
let x = 10;
console.log(x);